import asyncio
import websockets

# async def handler(websocket, path):
#     # This function will be called for each new connection
#     # You can communicate with the client using the `websocket` object
#     # For example, to send a message to the client, you would do:
#     await websocket.send("Hello, Client!")
#
# # Start the WebSocket server
# server = websockets.serve(handler, 'localhost', 12345)
#
# # Run the server indefinitely
# asyncio.get_event_loop().run_until_complete(server)
# asyncio.get_event_loop().run_forever()
